# v2.1.9 (2026-04-02)

## 文档

- README「部署」：明确 Pages 部署在 `main` / `lyl-dev-claude` / `pxf-dev-cursor` 推送时均可触发 CI。
- README「提交规范」：补充提交信息须符合 Conventional Commits，并与 `cursor_agent_workflow` 的 `AGENTS.md` 对齐；示例命令改为 `pxf-dev-cursor` 与规范式 `git commit -m`。

## 关联

- 方法论仓库已增加「fund 提交须同时满足本 README 提交规范表」的交叉引用（`architecture.md` / `AGENTS.md` / `.cursor/rules`）。
